---

---

<blockquote>
    <slot />
</blockquote>

<style>
    blockquote {
        font-family: var(--font-narrative); font-variation-settings: "SOFT" 50, "WONK" 0, "opsz" 36;
        font-weight: 300; font-size: var(--step-4); line-height: 1.1; color: var(--ink);
        position: relative; padding: var(--space-12) 0; text-align: center; grid-column: 2 / 12; width: 80%; margin: var(--space-8) auto;
    }
    blockquote::before {
        content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%;
        background: var(--cloud-dream); filter: blur(60px); opacity: 0.15; z-index: -1;
    }
</style>
